# Bug 445934  Layout linking broken in NS_OSSO disable-libxul configuration
--- mozilla/layout/build/Makefile.in.orig	2008-07-17 23:15:17.000000000 +0300
+++ mozilla/layout/build/Makefile.in	2008-07-18 09:29:42.000000000 +0300
@@ -238,10 +238,14 @@ ifdef MOZ_ENABLE_GTK2
 EXTRA_DSO_LDOPTS += $(MOZ_GTK2_LIBS) \
 		    -lXrender \
 		    $(NULL)
 endif
 
+ifdef NS_OSSO
+EXTRA_DSO_LDOPTS += -llocation -lgpsbt
+endif
+
 ifneq (,$(filter mac cocoa,$(MOZ_WIDGET_TOOLKIT)))
 EXTRA_DSO_LDOPTS += \
 	$(TK_LIBS) \
 	$(NULL)
 endif
